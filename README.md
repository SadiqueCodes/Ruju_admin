# Ruju Admin Panel (React + Vite)

Separate React admin app for Quran ayah ingestion.

## Configure

Edit `src/config.js`:

- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `ADMIN_EMAIL`

## Run

```bash
cd ruju-admin-panel
npm install
npm run dev
```

Open:

```text
http://localhost:5174
```
